# Docker
ARCH: amd64 # amd64 | arm64 | arm64-igpu
BASE_IMAGE: ros:humble

# ROS DDS
DDS: cyclone # cyclone | fastdds
ROS_DOMAIN_ID: 14

# Compose
PROJECT_NAME: vision-test
PROFILES: camera # center | camera | aruco | yolo | train | gui | stitch

# Build / Run
ACTION: run # build | run | both
